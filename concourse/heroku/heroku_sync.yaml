platform: linux
image_resource:
  type: docker-image
  source:
    repository: kissorjeyabalan/heroku-cli-maven
    tag: "1.0.1"
inputs:
- name: source
outputs:
- name: artifact
  path: artifacts/
caches:
  - path: maven
params:
  OUTPUT_NAME:
run:
  path: /bin/bash
  dir: source
  args:
    - sw-api-infra/concourse/heroku/build.sh
  #args:
  #  - -exc
  #  - |
  #    mvn install
  #    ls
  #    ls target/
  #    mv target/${OUTPUT_NAME} artifacts/
